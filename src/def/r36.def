Bootstrap: docker
From: rocker/verse:3.6.3

%environment
    export APPTAINER_BUILD_DATE=date

%files
    src/install/install-r-libs.sh
    src/install/install-r-radian-363.sh
    src/install/install-vscode.sh
    src/install/install-xet.sh

%post
    apt-get update && export DEBIAN_FRONTEND=noninteractive
    apt-get upgrade -y
    sh src/install/add_r_libs-select.sh
    sh src/install/add_radian-363.sh
    sh src/apptainer/ubuntu/install_vscode.sh
    apt-get autoremove -y && apt-get clean -y && rm -rf /var/lib/apt/lists/*

