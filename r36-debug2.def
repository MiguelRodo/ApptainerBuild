Bootstrap: localimage
From: r36-debugged.sif

%post
    install2.r --error --skipinstalled --ncpus -1 \
          languageserver \
          jsonlite \
          devtools \
          usethis \
      && install2.r --error --skipinstalled --ncpus -1 \
       --repos https://packagemanager.rstudio.com/cran/latest \
       renv \
      && rm -rf /tmp/downloaded_packages